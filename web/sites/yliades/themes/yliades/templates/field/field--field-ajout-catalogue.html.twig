<div class="container vue-catalogue">
        <div class="row text-center justify-content-center">
            {% for item in items %}
                    <div class="item col-12 col-sm-6 col-lg-4 col-xl-auto" {{ item.attributes }}>
                        {{ item.content }}
                    </div>
            {% endfor %}
        </div>
</div>