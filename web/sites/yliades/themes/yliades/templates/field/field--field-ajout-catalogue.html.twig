<div class="container vue-catalogue">
        <div class="row justify-content-between">
            {% for item in items %}
                    <div class="item col-12 col-sm-4 col-xl-auto" {{ item.attributes }}>
                        {{ item.content }}
                    </div>
            {% endfor %}
        </div>
</div>