<div class="container vue-catalogue">
        <div class="row">
            {% for item in items %}
                    <div class="item col-12 col-sm-4 col-xl" {{ item.attributes }}>
                        {{ item.content }}
                    </div>
            {% endfor %}
        </div>
</div>