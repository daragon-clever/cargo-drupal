<!-- Overlay à l'ouverture du menu -->
<div class="overlay js-toggle-menu" style="display: none;"> </div>

<!-- Header et navigation -->
{% include directory ~ '/partials/header.html.twig' %}

<div class="container">
    {% if page.highlighted %}
        <div class="row">
            <div class="col">
                {{ page.highlighted }}
            </div>
        </div>
    {% endif %}

    {% if page.content %}
        <div class="row">
            <div class="col">
                {{ page.content }}
            </div>
        </div>
    {% endif %}
</div>

<!-- Footer -->
{% if page.footer %}
    {{ page.footer }}
{% endif %}